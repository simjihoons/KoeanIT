console.log("Hello World!");

후 콘솔에서 node main.js 라고 실행하면 실행됨

==> node js 에는 자바스크립트 엔진이 있어서 브라우저 없이도 자바스크립트를 실행할 수 있음


node js 를 이용했을때나 웹으로 콘솔에서 확인할 수 있는 이유는
둘다 콘솔에 관련된 api가 있기 때문이다 이 둘의 api의 인터페이스가 동일하다.

콘솔을 언어자체에 포함되진않지만 통상적으로 많이 사용하기 때문에 콘솔에 관련된 api가 있는것이다.

자바스크립트의 공식 사이트 => https://developer.mozilla.org/en-US/
                         => mdn

