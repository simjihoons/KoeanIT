DML
	select
	insert
	update
	delete

조건식 
	참 또는 거짓 둘 중 하나의 결과가 나오는 식

WHERE
	>,<,=,>=,<=,  // 같지 않다 => !=,^=,<>
	and , or 
	
데이터 내보내기 result 창 드래그 -> 우클릭 내보내기 -> sql -> 폴더 지정
파일 편집 -> 다른이름으로 저장 -> ansi 변경 -> cmd -> hr 로그인 @하나 치고 파일 끌어오기
엔터 -> 그 후 commit; 을 해야 한다.(하지 않으면 임시 저장소에 있기때문에 적용이 안됨)